function add(x, y) {
    return x + y;
}

function division(x, y) {
    return x / y;
}



add(10, 20);        // stannar tills x + y ar utraknat

division(10, 2);    // stannar har tills x / y ar utraknat
add(100, 100);      // ......
division(100, 2);   // ......